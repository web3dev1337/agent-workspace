# Plan

1. Hide the tab buttons container (Start work / Resume) with `style="display: none;"`
2. Hide the resume tab panel with `style="display: none;"`
3. The "start" panel is already the default active panel, so it shows automatically
