# Task: Hide Resume Button in Quick Work Modal

Hide the Resume tab/button from the Quick Work (Add Worktree) modal.
Keep the code intact but hide it via CSS. Make Start Work the default view
without showing the tab selector.
