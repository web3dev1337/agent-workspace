User request (voice, 2026-08-21): the "project register of all GitHub projects" (descriptions, metadata, recently-edited, per-person visibility/permissions, periodic cross-machine sync via a private GitHub repo) was found already built inside stale open PR #1029 (JARVIS mega-branch, July 25). Extract JUST the Repo Atlas so it lands on main instead of living on a forgotten branch checkout that the `atlas` CLI wrapper happens to resolve.
